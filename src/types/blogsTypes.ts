export type BlogInput = {
    name: string,
    description: string,
    websiteUrl: string
}
export type BlogOutput = {
    id: string,
    name: string,
    description: string,
    websiteUrl: string
}